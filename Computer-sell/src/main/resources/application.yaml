server:
  port: 8080

spring:
  application:
    name: Computer-sell
  profiles:
    active: "@spring.profile.active@"
  
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        use_sql_comments: true

management:
  endpoint:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: INFO
    web: INFO

seller:
  name: "CÔNG TY TNHH THƯƠNG MẠI VÀ DỊCH VỤ THComputer"
  tax: "01234567891"
  address: "123 3/2 Phường Xuân Khánh, Quận Ninh Kiều, TP. Cần Thơ"
  phone: "0287123456"

vnpay:
  tmn-code: "3WKQT5FR"
  hash-secret: "0UE2G4R0Q2Y5CM0V1I5QJ20UPNIHJMDE"
  return-url: "http://localhost:5173/payment/vnpay-return"
  ipn-url: "http://localhost:8080/api/payments/vnpay/ipn"
  pay-url: "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"

# VietQR Configuration - Thay đổi thông tin ngân hàng của bạn
vietqr:
  bank-id: "970436"                    # Mã ngân hàng (MB Bank: 970422, Vietcombank: 970436, etc.)
  account-no: "1026162961"             # Số tài khoản ngân hàng
  account-name: "HO KIM TRONG"  # Tên chủ tài khoản
  template: "compact2"                 # Template QR (compact, compact2, qr_only, print)

# AI Provider Configuration
ai:
  provider: "${AI_PROVIDER:groq}"

# Groq API Configuration
groq:
  api-key: "${GROQ_API_KEY}"
  base-url: "https://api.groq.com/openai/v1"
  chat-model: "llama-3.1-70b-versatile"
  max-tokens: 2048
  temperature: 0.7

# Gemini API Configuration
gemini:
  api:
    key: "${GEMINI_API_KEY:AIzaSyAQR7IRMQA8cdWhyU5ZnmLuTjp_u3fPOyg}"
  base-url: "https://generativelanguage.googleapis.com/v1beta"
  embedding-model: "text-embedding-004"

# OpenAI API Configuration for RAG Chatbot
openai:
  api-key: "${OPENAI_API_KEY}"
  embedding-model: "text-embedding-3-large"
  chat-model: "gpt-4o-mini"
  base-url: "https://api.openai.com/v1"
  embedding-dimension: 1536
  max-tokens: 2000
  temperature: 0.7

# SendGrid Configuration
sendgrid:
  api-key: "${SENDGRID_API_KEY}"

